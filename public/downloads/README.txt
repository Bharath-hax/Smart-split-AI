Place your built SplitSettle AI APK file here as "smartsplit.apk".

The "Get App" button in the app header links to /downloads/smartsplit.apk.
Build the APK using Bubblewrap (see the separate APK build prompt).

Until the real APK is placed here, the download link will 404 — that's expected.
